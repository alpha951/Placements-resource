CREATE DATABASE myDB;
USE myDB;
CREATE TABLE student (
	id INT PRIMARY KEY,
    name VARCHAR(255)
);
INSERT INTO student VALUES(1, 'ANKIT');
SELECT * FROM student;
INSERT INTO student VALUES(2, 'Keshav');
SELECT * FROM student;
-- DROP database if exists myDB;















